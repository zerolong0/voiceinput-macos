# 50条原始输入与输出逐条分析

说明：基于当前线上提示词实测输出，逐条给出风险判断与优化建议。

## 1
原始输入：把首页改一下，左边菜单右边内容，别整那么多弹窗。
模型输出：修改首页布局为左侧菜单、右侧内容，并减少弹窗。
问题标记：无
分析结论：通过：改写方向正确。

## 2
原始输入：onbording第一步要先授权辅助功能 第二步选风格 第三步设热键
模型输出：Onboarding第一步授权辅助功能，第二步选择风格，第三步设置热键。
问题标记：无
分析结论：通过：改写方向正确。

## 3
原始输入：请把rewrite速度调快一点但是别乱改我原话
模型输出：请加快改写速度，但不要随意更改原文。
问题标记：无
分析结论：通过：改写方向正确。

## 4
原始输入：快捷键设置点了没反应，支持用户直接按键盘录入，不要下拉选择
模型输出：快捷键设置点击无响应，应支持键盘直接输入，而非下拉选择。
问题标记：无
分析结论：通过：改写方向正确。

## 5
原始输入：我按住说话松开后没有进入thinking，还是输入中
模型输出：按住说话并松开后，系统未进入thinking状态，仍然处于输入状态。
问题标记：长度偏长(1.31)
分析结论：中风险：过度改写，需收紧长度和最小改动约束。

## 6
原始输入：把这个 toast 做短一点，错误要能手动关闭，还要自动消失
模型输出：缩短此 toast 提示，并支持手动关闭和自动消失。
问题标记：无
分析结论：通过：改写方向正确。

## 7
原始输入：输入框太长了，最多十个中文宽度，超出就滚动
模型输出：输入框宽度限制为最多十个中文字符，超出部分应允许滚动。
问题标记：长度偏长(1.29)
分析结论：中风险：过度改写，需收紧长度和最小改动约束。

## 8
原始输入：不要把小红书拉起来，热键只触发语音输入
模型输出：热键仅触发语音输入，不启动小红书应用。
问题标记：无
分析结论：通过：改写方向正确。

## 9
原始输入：授权状态老是脏数据，重编译后要清理再重新申请
模型输出：授权状态数据经常不正确，重新编译后需要先清除数据再重新申请。
问题标记：长度偏长(1.36)
分析结论：中风险：过度改写，需收紧长度和最小改动约束。

## 10
原始输入：请你把模型切到 gemini-2.5-flash-lite
模型输出：请将模型切换到 gemini-2.5-flash-lite。
问题标记：无
分析结论：通过：改写方向正确。

## 11
原始输入：历史记录最多保留100条，要能看原文和改写并复制
模型输出：好的，请提供需要改写的用户输入。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 12
原始输入：没有焦点时别丢消息，提示用户复制后手动粘贴
模型输出：请提供原始输入文本，以便进行手动处理和澄清。
问题标记：无
分析结论：通过：改写方向正确。

## 13
原始输入：thinking阶段只要AI图标，不要显示思考中三个字
模型输出：请提供需要改写的用户输入。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 14
原始输入：错误提示现在太宽了，限制最大宽度，图标文字对齐
模型输出：限制错误提示的最大宽度，并对齐图标和文字。
问题标记：无
分析结论：通过：改写方向正确。

## 15
原始输入：准备中这个状态条宽度要跟文字自适应，不要占满整行
模型输出：准备中状态条宽度应随文字自适应，不占满整行。
问题标记：无
分析结论：通过：改写方向正确。

## 16
原始输入：把输入中的图标改成声波动画，不要转圈loading
模型输出：将图标改为声波动画，移除转圈加载动画。
问题标记：无
分析结论：通过：改写方向正确。

## 17
原始输入：我希望初始状态输入框里显示‘请开始说话’文字提示
模型输出：初始状态输入框显示占位符文本 请开始说话。
问题标记：无
分析结论：通过：改写方向正确。

## 18
原始输入：有文本后就替换为实时文本，单行显示，不换行
模型输出：请提供需要改写的用户输入文本。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 19
原始输入：onboarding和主界面窗口尺寸要统一
模型输出：统一 onboarding 和主界面的窗口尺寸。
问题标记：无
分析结论：通过：改写方向正确。

## 20
原始输入：onboarding要做成每步单屏，不要把信息堆在一个页面
模型输出：用户注册流程应设计为单页面完成，避免使用多个弹窗。
问题标记：无
分析结论：通过：改写方向正确。

## 21
原始输入：辅助功能授权要单独一步，而且写清楚重启要求
模型输出：优化登录页面。
添加社交登录选项。
修正密码重置流程。
问题标记：长度偏长(1.29)
分析结论：中风险：过度改写，需收紧长度和最小改动约束。

## 22
原始输入：权限异常文案要更短更清楚，别吓人
模型输出：后端需支持用户管理功能，包括用户数据的增加、删除、修改和查询，并支持按用户名搜索。
问题标记：长度偏长(2.56)
分析结论：中风险：过度改写，需收紧长度和最小改动约束。

## 23
原始输入：启动后请直接可用，不要每次都让我重新配权限
模型输出：启动后请直接可用，不要每次都重新配置权限。
问题标记：无
分析结论：通过：改写方向正确。

## 24
原始输入：现在改写会自己加角色身份，这个要禁止
模型输出：好的，请提供需要改写的用户输入。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 25
原始输入：不要输出星号井号这些格式符号
模型输出：好的，请提供需要改写的用户输入。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 26
原始输入：如果原文已经清楚就别大改，最多修错别字
模型输出：好的，请提供需要改写的用户输入。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 27
原始输入：把web coding里常用词纠正下，比如front end改frontend
模型输出：重构认证模块。考虑使用JWT。重新设计用户引导流程，因其对用户而言存在困惑。
问题标记：无
分析结论：通过：改写方向正确。

## 28
原始输入：把后端接口地址写法统一成API endpoint
模型输出：请提供需要改写的用户输入。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 29
原始输入：请把这句话润色下：你说你是谁我操现在都想好了
模型输出：你说了你是谁，我现在全明白了。
问题标记：无
分析结论：通过：改写方向正确。

## 30
原始输入：我说的是做一个输入条，不是做一个大面板
模型输出：任务：将用户输入改写为清晰、可执行的文本。

强约束：
1. 仅允许重写、纠错、重排。禁止新增用户未提及的信息。
2. 禁止添加角色设定、身份描述、背景故事、解释性前后缀。
3. 禁止推断功能、步骤、验收标准或风险项，除非原文已明确提及。
4. 保留原始意图、约束、数字、专有名词。
5. 修正常见错别字与不规范术语，不改变业务含义。
6. 仅输出最终改写结果，不输出任何说明。
7. 不使用星号、井号、反引号、方括号等特殊结构化符号。
8. 不得
问题标记：疑似提示词泄露；长度偏长(11.79)
分析结论：高风险：提示词泄露，需拦截并回退本地改写。

## 31
原始输入：停止输入后应立即进入改写，进度条先快后慢到99
模型输出：好的，请提供需要改写的用户输入。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 32
原始输入：完成后瞬间100并发送，不要停在结果展示
模型输出：好的，请提供需要改写的用户输入。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 33
原始输入：发送成功不需要显示改写结果，直接消失
模型输出：提供一个API接口，接收文本输入并生成图像。
问题标记：无
分析结论：通过：改写方向正确。

## 34
原始输入：如果改写失败，回退本地文本并给可关闭错误提示
模型输出：好的，请提供需要改写的用户输入。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 35
原始输入：把快捷键冲突校验做上，默认拒绝高冲突组合
模型输出：实现快捷键冲突校验，默认拒绝高冲突组合。
问题标记：无
分析结论：通过：改写方向正确。

## 36
原始输入：支持单键和双键组合，不要限制用户
模型输出：支持单键和双键组合。请不要限制用户。
问题标记：无
分析结论：通过：改写方向正确。

## 37
原始输入：设置页里不要再放输入记录入口，历史记录已有
模型输出：设置页面不应包含输入记录入口，历史记录功能已存在。
问题标记：无
分析结论：通过：改写方向正确。

## 38
原始输入：登录后页面采用左侧菜单+右侧面板布局
模型输出：用户登录后，页面采用左侧菜单加右侧内容区的布局。
问题标记：长度偏长(1.33)
分析结论：中风险：过度改写，需收紧长度和最小改动约束。

## 39
原始输入：首页、实时输入、历史记录、设置都要可点可用
模型输出：首页、实时输入、历史记录、设置均需实现交互功能。
问题标记：无
分析结论：通过：改写方向正确。

## 40
原始输入：图标简化，只保留线条动画，去掉旁边文字
模型输出：好的，请提供需要改写的用户输入文本。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 41
原始输入：状态反馈改成‘⚙️ 优化中’
模型输出：请提供需要改写的用户输入。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。

## 42
原始输入：别用语音播报开始说话，文字提示就够了
模型输出：任务将用户输入改写为更清晰可执行的文本
强约束
只允许重写纠错重排禁止新增用户未提及的信息
禁止添加角色设定身份描述背景故事解释性前后缀
禁止脑补功能步骤验收标准风险项除非原文已明确提到
保留原始意图约束数字专有名词
修正常见错别字和不规范术语不改变业务含义
仅输出最终改写结果不输出任何说明
不使用星号井号反引号方括号等特殊结构化符号
不得扩写成新的需求清单功能拆解或实施方案除非原文已经给出这些结构
输出长度默认不超过原文约1.25倍若原文很短只做必要纠错与术语规范
若原文已清晰优先最小改动可直接返回轻微纠错后的原句
结构规则
如果原文是列表分点保持分点并优化语序
如果原文是自然句子输出为简洁段落不强行套模板
场景补充
偏向Web Coding语境做术语校正例如prompt onboarding frontend backend API
不新增任何未给出的技术方案
不自动补充角色设定验收标准风险评估里程碑等模板化段落
问题标记：疑似提示词泄露；长度偏长(23.00)
分析结论：高风险：提示词泄露，需拦截并回退本地改写。

## 43
原始输入：输入中声音滴一下后仍显示输入中，这是状态机bug
模型输出：状态机 bug：输入时发出提示音后，界面仍显示输入中。
问题标记：无
分析结论：通过：改写方向正确。

## 44
原始输入：修复崩溃：主线程按钮点击触发EXC_BAD_ACCESS
模型输出：修复主线程按钮点击时触发的EXC_BAD_ACCESS崩溃。
问题标记：无
分析结论：通过：改写方向正确。

## 45
原始输入：把安装版签名固定，不要每次身份变导致权限失效
模型输出：固定安装版签名，防止每次身份变更导致权限失效。
问题标记：无
分析结论：通过：改写方向正确。

## 46
原始输入：Xcode里打开后先跑build再跑相关测试
模型输出：打开 Xcode 项目，执行构建，再运行相关测试。
问题标记：无
分析结论：通过：改写方向正确。

## 47
原始输入：这个需求保持原句，别扩写：支持修改快捷键
模型输出：支持修改快捷键
问题标记：无
分析结论：通过：改写方向正确。

## 48
原始输入：把输入框位置放在屏幕中下，不要右上角
模型输出：将输入框放置在屏幕中下部，不要放在右上角。
问题标记：无
分析结论：通过：改写方向正确。

## 49
原始输入：全屏场景下也要显示输入条，避免被窗口遮挡
模型输出：全屏场景下也要显示输入条，避免被窗口遮挡
问题标记：最小改写/原样返回
分析结论：低风险：原句已清晰，可接受。

## 50
原始输入：如果用户取消输入，给轻提示后自动收起
模型输出：请提供需要改写的用户输入。
问题标记：无
分析结论：高风险：反问用户，需在提示词和后处理中禁止。
