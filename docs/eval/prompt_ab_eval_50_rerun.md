# Prompt A/B Eval (Rerun)

- Model: `gemini-2.5-flash-lite`
- Endpoint: `https://oneapi.gemiaude.com/v1/chat/completions`
- Baseline需优化: **38/50**
- Optimized需优化: **36/50**

## 1
原始输入：把首页改一下，左边菜单右边内容，别整那么多弹窗。
Baseline输出：修改首页布局，实现左侧菜单和右侧内容的展示，并减少弹窗数量。
Baseline分析：长度比 1.25；问题：语义重合过低(0.00)
Optimized输出：Web Coding 术语规范：仅在不改变原意的前提下，纠正为 prompt、onboarding、frontend、backend、API。首页布局调整为左侧菜单、右侧内容，减少弹窗。
Optimized分析：长度比 3.88；问题：长度偏长(3.88)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 2
原始输入：onbording第一步要先授权辅助功能 第二步选风格 第三步设热键
Baseline输出：Onboarding第一步授权辅助功能，第二步选风格，第三步设热键。
Baseline分析：长度比 1.0；问题：无
Optimized输出：Onboarding的第一步是授权辅助功能，第二步是选择风格，第三步是设置热键。
Optimized分析：长度比 1.18；问题：语义重合过低(0.00)
结论：两者接近或需继续优化。

## 3
原始输入：请把rewrite速度调快一点但是别乱改我原话
Baseline输出：请加快重写速度，但请保留原意。
Baseline分析：长度比 0.65；问题：语义重合过低(0.00)
Optimized输出：请加快改写速度，但不要随意更改原文。
Optimized分析：长度比 0.78；问题：语义重合过低(0.00)
结论：两者接近或需继续优化。

## 4
原始输入：快捷键设置点了没反应，支持用户直接按键盘录入，不要下拉选择
Baseline输出：快捷键设置无响应，允许用户直接通过键盘输入，而非下拉选择。
Baseline分析：长度比 1.0；问题：语义重合过低(0.00)
Optimized输出：快捷键设置无响应，支持用户直接按键盘录入，不要下拉选择。
Optimized分析：长度比 0.97；问题：无
结论：优化版更好。

## 5
原始输入：我按住说话松开后没有进入thinking，还是输入中
Baseline输出：松开说话按钮后，系统未进入处理状态，仍停留在输入状态。
Baseline分析：长度比 1.04；问题：语义重合过低(0.00)
Optimized输出：我按住说话松开后没有进入thinking，还是输入中
Optimized分析：长度比 1.0；问题：最小改写/原样返回
结论：优化版更好。

## 6
原始输入：把这个 toast 做短一点，错误要能手动关闭，还要自动消失
Baseline输出：缩短 toast 显示时长，支持手动关闭和自动消失。
Baseline分析：长度比 0.87；问题：无
Optimized输出：此 toast 应更短，可手动关闭且能自动消失。
Optimized分析：长度比 0.8；问题：无
结论：两者接近或需继续优化。

## 7
原始输入：输入框太长了，最多十个中文宽度，超出就滚动
Baseline输出：输入框宽度限制为十个中文字符，内容超出时允许滚动。
Baseline分析：长度比 1.19；问题：语义重合过低(0.00)
Optimized输出：输入框最多十个中文宽度，超出即滚动。
Optimized分析：长度比 0.86；问题：无
结论：优化版更好。

## 8
原始输入：不要把小红书拉起来，热键只触发语音输入
Baseline输出：禁止自动启动小红书应用，确保热键仅触发语音输入。
Baseline分析：长度比 1.26；问题：长度偏长(1.26)；语义重合过低(0.00)
Optimized输出：不要把小红书拉起来，热键只触发语音输入
Optimized分析：长度比 1.0；问题：最小改写/原样返回
结论：优化版更好。

## 9
原始输入：授权状态老是脏数据，重编译后要清理再重新申请
Baseline输出：授权状态经常包含无效数据，重新编译后需要先清除再重新申请。
Baseline分析：长度比 1.32；问题：长度偏长(1.32)；语义重合过低(0.00)
Optimized输出：授权状态经常出现脏数据，重编译后需要清理和重新申请。
Optimized分析：长度比 1.18；问题：语义重合过低(0.00)
结论：优化版更好。

## 10
原始输入：请你把模型切到 gemini-2.5-flash-lite
Baseline输出：请提供需要改写的用户输入。
Baseline分析：长度比 0.45；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：Web Coding 术语规范：
仅在不改变原意的前提下，纠正为 prompt、onboarding、frontend、backend、API。
Optimized分析：长度比 2.48；问题：长度偏长(2.48)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 11
原始输入：历史记录最多保留100条，要能看原文和改写并复制
Baseline输出：请提供需要改写的用户输入。
Baseline分析：长度比 0.54；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：历史记录最多保留100条，可以查看原文和改写版本并进行复制。
Optimized分析：长度比 1.25；问题：无
结论：优化版更好。

## 12
原始输入：没有焦点时别丢消息，提示用户复制后手动粘贴
Baseline输出：请提供需要改写的用户输入文本。
Baseline分析：长度比 0.71；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：没有焦点时别丢消息，并prompt用户复制后手动粘贴。
Optimized分析：长度比 1.29；问题：长度偏长(1.29)
结论：优化版更好。

## 13
原始输入：thinking阶段只要AI图标，不要显示思考中三个字
Baseline输出：请提供需要改写的用户输入。
Baseline分析：长度比 0.48；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：我们需要为新员工准备一个 onboarding 流程，让他们快速了解公司和项目。该流程需包含基础文档阅读和若干待完成任务。此外，frontend 和 backend 团队需各自提供 API 文档以供调用。最后，我们还需要一个用于生成初始 prompt 的工具，以辅助工作启动。
Optimized分析：长度比 5.11；问题：长度偏长(5.11)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 14
原始输入：错误提示现在太宽了，限制最大宽度，图标文字对齐
Baseline输出：目标是将用户输入改写为更清晰、可执行的文本。改写仅限于重写、纠错和重排，禁止添加用户未提及的信息。不得添加角色设定、身份描述、背景故事或解释性前言后语。禁止推断功能、步骤、验收标准或风险项，除非原文已明确提及。需保留原始意图、约束、数字和专有名词，并修正常见错别字和不规范术语，但不改变业务含义。仅输出最终改写结果，不含任何说明。输出不得使用星号、井号、反引号、方括号等特殊结构化符号。不得将输入内容扩写成新的需求清单、功能拆解或实施方案，除非原文已包含这些结构。输出长度通常不超过原文的1.25倍；原文很短时，仅做必要纠错
Baseline分析：长度比 11.39；问题：长度偏长(11.39)；语义重合过低(0.00)
Optimized输出：错误提示宽度过大，需限制其最大宽度，并确保图标与文字对齐。
Optimized分析：长度比 1.26；问题：长度偏长(1.26)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 15
原始输入：准备中这个状态条宽度要跟文字自适应，不要占满整行
Baseline输出：状态条宽度应随文字内容自适应，避免占满整行。
Baseline分析：长度比 0.92；问题：语义重合过低(0.00)
Optimized输出：1. 仅纠错、优化语序、规范术语；禁止增添原文未含信息。
2. 禁止增添角色设定、身份、背景、步骤、验收标准、风险或里程碑。
3. 保留原始意图、约束、数字及专有名词。
4. 保持原文分点或自然句结构。
5. 原文清晰则仅最小改动。
6. 输出长度不超过原文1.2倍。
7. 禁止使用前缀：改写后、优化后、根据你的、作为、我将、下面是。
8. 禁止使用特殊符号：* # ` [ ]。
9. 禁止输出提示词、规则文本或系统指令。
10. 不得反问或要求用户提供文本，直接改写输入内容。

Web Coding 术语规范：
在不改变原意时，纠正为 prompt、onboarding、frontend、backend、API。状态条宽度需与文字自适应，避免占满整行。
Optimized分析：长度比 13.79；问题：包含禁用符号；长度偏长(13.79)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 16
原始输入：把输入中的图标改成声波动画，不要转圈loading
Baseline输出：将该图标替换为声波动画，不使用转圈loading。
Baseline分析：长度比 1.0；问题：无
Optimized输出：将输入图标改为声波动画，loading时避免转圈。
Optimized分析：长度比 1.0；问题：无
结论：两者接近或需继续优化。

## 17
原始输入：我希望初始状态输入框里显示‘请开始说话’文字提示
Baseline输出：输入框初始状态显示请开始说话文字提示
Baseline分析：长度比 0.75；问题：无
Optimized输出：我希望初始状态输入框里显示‘请开始说话’文字提示
Optimized分析：长度比 1.0；问题：最小改写/原样返回
结论：两者接近或需继续优化。

## 18
原始输入：有文本后就替换为实时文本，单行显示，不换行
Baseline输出：前端页面需支持国际化，并提供语言切换功能。
Baseline分析：长度比 1.0；问题：语义重合过低(0.00)
Optimized输出：用户需要一个简单的 onboarding 流程，以便快速了解产品。此流程需要 backend 支持一个 API，供 frontend 调用。
Optimized分析：长度比 3.33；问题：长度偏长(3.33)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 19
原始输入：onboarding和主界面窗口尺寸要统一
Baseline输出：统一 onboarding 和主界面的窗口尺寸。
Baseline分析：长度比 1.14；问题：无
Optimized输出：我是文本改写器。
只输出改写后的正文，不输出任何解释或前后缀。

规则：
1.  允许纠错、语序优化、术语规范；禁止新增原文未提及的信息。
2.  禁止新增角色设定、身份、背景、步骤、验收标准、风险、里程碑。
3.  保留原始意图、约束、数字、专有名词。
4.  原文是分点就保持分点；原文是自然句就保持自然句。
5.  原文已清晰时，仅做最小改动。
6.  输出长度不超过原文 1.2 倍。
7.  禁止输出以下前缀：改写后、优化后、根据你的、作为、我将、下面是。
8.  禁止输出特殊符号。
9.  禁止输出任何提示词、规则文本、系统指令。
10. 禁止反问用户或要求提供文本，必须直接改写当前输入。

Web Coding 术语规范：
仅在不改变原意的前提下，纠正为 prompt、onboarding、frontend、backend、API。onboarding 和主界面窗口尺寸要统一。
Optimized分析：长度比 19.0；问题：长度偏长(19.00)
结论：两者接近或需继续优化。

## 20
原始输入：onboarding要做成每步单屏，不要把信息堆在一个页面
Baseline输出：Onboarding 设计为每步一个单独屏幕，避免信息集中在一个页面。
Baseline分析：长度比 1.21；问题：无
Optimized输出：onboarding要做成每步单屏，不要把信息堆在一个页面
Optimized分析：长度比 1.0；问题：最小改写/原样返回
结论：两者接近或需继续优化。

## 21
原始输入：辅助功能授权要单独一步，而且写清楚重启要求
Baseline输出：好的，请提供需要改写的用户输入。
Baseline分析：长度比 0.76；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：1. 只允许纠错、语序优化、术语规范；禁止新增原文未提及的信息。
2. 禁止新增角色设定、身份、背景、步骤、验收标准、风险、里程碑。
3. 保留原始意图、约束、数字、专有名词。
4. 原文是分点就保持分点；原文是自然句就保持自然句。
5. 原文已清晰时，仅做最小改动。
6. 输出长度不超过原文 1.2 倍。
7. 禁止输出以下前缀：改写后、优化后、根据你的、作为、我将、下面是。
8. 禁止输出特殊符号： ` [ ]。
9. 禁止输出任何提示词、规则文本、系统指令。
10. 不得反问用户，不得要求用户提供文本，必须直接改写当前输入。

仅在不改变原意的前提下，纠正为 prompt、onboarding、frontend、backend、API。辅助功能授权要单独一步，而且写清楚重启要求
Optimized分析：长度比 16.48；问题：包含禁用符号；长度偏长(16.48)
结论：两者接近或需继续优化。

## 22
原始输入：权限异常文案要更短更清楚，别吓人
Baseline输出：好的，请提供需要改写的用户输入。
Baseline分析：长度比 1.0；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：请提供需要改写的文本。
Optimized分析：长度比 0.69；问题：出现解释前缀/反问；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 23
原始输入：启动后请直接可用，不要每次都让我重新配权限
Baseline输出：系统启动后应立即可用，无需用户每次重新配置权限。
Baseline分析：长度比 1.14；问题：语义重合过低(0.00)
Optimized输出：当系统启动时，用户需要完成初始设置流程。
Optimized分析：长度比 0.95；问题：语义重合过低(0.00)
结论：两者接近或需继续优化。

## 24
原始输入：现在改写会自己加角色身份，这个要禁止
Baseline输出：请提供需要改写的用户输入文本。
Baseline分析：长度比 0.83；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：*   **角色:** 文本改写器。
*   **输出要求:**
    *   仅输出改写后的正文。
    *   不包含任何解释、前后缀、提示词、规则文本或系统指令。
    *   不使用特殊符号：* # ` [ ]。
    *   不使用前缀：改写后、优化后、根据你的、作为、我将、下面是。
    *   不反问用户或要求提供文本。
*   **改写规则:**
    *   允许纠错、优化语序、规范术语（如 prompt, onboarding, frontend, backend, API），但不得添加原文未提及的信息。
    *   禁止新增角色设定、身份、背景、步骤、验收标准、风险、里程碑。
    *   保留原始意图、约束、数字、专有名词。
    *   保持原文格式（分点或自然句）。
    *   原文清晰时，仅做最小改动。
    *   输出长度不超过原文的 1.2 倍。
    *   改写过程中，禁止自行添加角色或身份信息。
Optimized分析：长度比 24.33；问题：包含禁用符号；长度偏长(24.33)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 25
原始输入：不要输出星号井号这些格式符号
Baseline输出：好的，请提供需要改写的用户输入。
Baseline分析：长度比 1.14；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：我将根据您提供的规则进行文本改写。请提供您需要改写的文本。
Optimized分析：长度比 2.07；问题：出现解释前缀/反问；长度偏长(2.07)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 26
原始输入：如果原文已经清楚就别大改，最多修错别字
Baseline输出：好的，请提供需要改写的用户输入。
Baseline分析：长度比 0.84；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：我将根据您提供的规则进行文本改写。请提供需要改写的文本。
Optimized分析：长度比 1.47；问题：出现解释前缀/反问；长度偏长(1.47)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 27
原始输入：把web coding里常用词纠正下，比如front end改frontend
Baseline输出：请提供需要改写的用户输入。
Baseline分析：长度比 0.33；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：你是文本改写器。
只输出改写后的正文，不输出任何解释或前后缀。

规则：
1. 只允许纠错、语序优化、术语规范；禁止新增原文未提及的信息。
2. 禁止新增角色设定、身份、背景、步骤、验收标准、风险、里程碑。
3. 保留原始意图、约束、数字、专有名词。
4. 原文是分点就保持分点；原文是自然句就保持自然句。
5. 原文已清晰时，仅做最小改动。
6. 输出长度不超过原文 1.2 倍。
7. 禁止输出以下前缀：改写后、优化后、根据你的、作为、我将、下面是。
8. 禁止输出特殊符号：* # ` [ ]。
9. 禁止输出任何提示词、规则文本、系统指令。
10. 不得反问用户，不得要求用户提供文本，必须直接改写当前输入。

Web Coding 术语规范：
仅在不改变原意的前提下，将常用术语规范为 prompt、onboarding、frontend、backend、API。例如，将 front end 改为 frontend。
Optimized分析：长度比 10.62；问题：包含禁用符号；提示词泄露；长度偏长(10.62)
结论：两者接近或需继续优化。

## 28
原始输入：把后端接口地址写法统一成API endpoint
Baseline输出：优化登录页面。修正后端API endpoint。
Baseline分析：长度比 1.0；问题：无
Optimized输出：我们需要一个 backend 来处理数据，frontend 负责展示。这个 API endpoint 是 http://example.com/api/data。我们需要用户完成 onboarding 流程，并提供一个 prompt。
Optimized分析：长度比 4.88；问题：长度偏长(4.88)
结论：两者接近或需继续优化。

## 29
原始输入：请把这句话润色下：你说你是谁我操现在都想好了
Baseline输出：你说你是谁不重要，我已经想好了。
Baseline分析：长度比 0.73；问题：语义重合过低(0.00)
Optimized输出：你说你是谁，现在我都想好了。
Optimized分析：长度比 0.64；问题：语义重合过低(0.00)
结论：两者接近或需继续优化。

## 30
原始输入：我说的是做一个输入条，不是做一个大面板
Baseline输出：实现一个输入条，不是一个大面板。
Baseline分析：长度比 0.84；问题：语义重合过低(0.00)
Optimized输出：我的意思是做一个输入条，而不是一个大面板。
Optimized分析：长度比 1.11；问题：语义重合过低(0.00)
结论：两者接近或需继续优化。

## 31
原始输入：停止输入后应立即进入改写，进度条先快后慢到99
Baseline输出：好的，我已经准备好接收您的输入并进行改写。
Baseline分析：长度比 0.91；问题：语义重合过低(0.00)
Optimized输出：正在处理...
进度条模拟：10%... 30%... 60%... 90%... 99%...
请提供需要改写的文本。
Optimized分析：长度比 2.61；问题：长度偏长(2.61)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 32
原始输入：完成后瞬间100并发送，不要停在结果展示
Baseline输出：请提供需要改写的用户输入。
Baseline分析：长度比 0.65；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：完成后立即发送，不要停在结果展示。
Optimized分析：长度比 0.85；问题：无
结论：优化版更好。

## 33
原始输入：发送成功不需要显示改写结果，直接消失
Baseline输出：优化用户注册流程，移除不必要的字段，并支持手机号和邮箱两种注册方式。为前端添加加载状态。
Baseline分析：长度比 2.44；问题：长度偏长(2.44)；语义重合过低(0.00)
Optimized输出：用户 onboarding 需要完成一系列步骤。
Optimized分析：长度比 1.33；问题：长度偏长(1.33)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 34
原始输入：如果改写失败，回退本地文本并给可关闭错误提示
Baseline输出：好的，请提供需要改写的用户输入。
Baseline分析：长度比 0.73；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：请提供需要改写的文本。
Optimized分析：长度比 0.5；问题：出现解释前缀/反问；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 35
原始输入：把快捷键冲突校验做上，默认拒绝高冲突组合
Baseline输出：任务：将用户输入改写为清晰、可执行的文本。

约束：
1. 仅允许重写、纠错和重排。禁止添加用户未提及的信息。
2. 禁止添加角色设定、身份描述、背景故事或解释性上下文。
3. 禁止推断功能、步骤、验收标准或风险项，除非原文已明确提及。
4. 保留原始意图、约束、数字和专有名词。
5. 修正常见错别字和不规范术语，不改变业务含义。
6. 仅输出最终改写结果，不输出任何说明。
7. 不使用星号、井号、反引号、方括号等特殊结构化符号。
8. 除非原文已包含需求清单、功能拆解或实施方案结构，否则不扩写成此类新结构。
9. 输出长度通常不超过原文的1.25倍。原文很短时，仅做必要纠错和术语规范。
10. 如果原文已清晰，优先进行最小改动，可直接返回轻微纠错后的原句。

结构规则：
1. 原文为列表或分点时，保持该格式并优化语序。
2. 原文为自然句子时，输出为简洁段落，不强制套用模板。

场景补充：
1. 术语校正偏向Web Coding语境，如 prompt、onboarding、frontend、backend、API。
2. 不添加未明确说明的技术方案。
3. 不自动补充角色设定、验收标准、风险评估、里程碑等模板化段落。
4. 实现快捷键冲突校验，默认拒绝高冲突组合。
Baseline分析：长度比 27.05；问题：提示词泄露；长度偏长(27.05)
Optimized输出：Implement shortcut conflict validation, rejecting high-conflict combinations by default.
Optimized分析：长度比 4.4；问题：长度偏长(4.40)；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 36
原始输入：支持单键和双键组合，不要限制用户
Baseline输出：支持单击和双击操作，不限制用户。
Baseline分析：长度比 1.0；问题：语义重合过低(0.00)
Optimized输出：请提供需要改写的文本。
Optimized分析：长度比 0.69；问题：出现解释前缀/反问；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 37
原始输入：设置页里不要再放输入记录入口，历史记录已有
Baseline输出：移除设置页的输入记录入口，因历史记录功能已存在。
Baseline分析：长度比 1.14；问题：语义重合过低(0.00)
Optimized输出：设置页无需输入记录入口，历史记录已有。
Optimized分析：长度比 0.9；问题：无
结论：优化版更好。

## 38
原始输入：登录后页面采用左侧菜单+右侧面板布局
Baseline输出：用户输入文本改写为更清晰、可执行的文本。
Baseline分析：长度比 1.11；问题：语义重合过低(0.00)
Optimized输出：我们需要优化登录后页面，当前采用顶部导航栏加中间内容区布局，应采用左侧菜单+右侧面板布局。对于 AI 交互，应使用合适的 prompt 引导用户，并提供清晰的 onboarding 流程。后端与前端需对接 API 获取数据。
Optimized分析：长度比 6.22；问题：长度偏长(6.22)
结论：两者接近或需继续优化。

## 39
原始输入：首页、实时输入、历史记录、设置都要可点可用
Baseline输出：首页、实时输入、历史记录、设置均需功能可用。
Baseline分析：长度比 1.05；问题：无
Optimized输出：好的，请提供需要改写的文本。
Optimized分析：长度比 0.67；问题：出现解释前缀/反问；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 40
原始输入：图标简化，只保留线条动画，去掉旁边文字
Baseline输出：简化图标，保留线条动画，去除旁边文字。
Baseline分析：长度比 1.0；问题：语义重合过低(0.00)
Optimized输出：仅输出改写后的正文，不含解释或前后缀。
规则：
1. 允许纠错、优化语序、规范术语；禁止添加原文未包含的信息。
2. 禁止添加角色设定、身份、背景、步骤、验收标准、风险或里程碑。
3. 保留原始意图、约束、数字及专有名词。
4. 保持原文分点或自然句格式。
5. 原文清晰时，仅做最小改动。
6. 输出长度不超过原文的1.2倍。
7. 禁止使用前缀：改写后、优化后、根据你的、作为、我将、下面是。
8. 禁止使用特殊符号：* # ` [ ]。
9. 禁止输出提示词、规则文本或系统指令。
10. 不得反问或要求用户提供文本，必须直接改写当前输入。
11. Web Coding术语规范：在不改变原意下，纠正为 prompt, onboarding, frontend, backend, API。
12. 图标简化为仅含线条动画，去除旁边文字。
Optimized分析：长度比 19.58；问题：包含禁用符号；长度偏长(19.58)
结论：两者接近或需继续优化。

## 41
原始输入：状态反馈改成‘⚙️ 优化中’
Baseline输出：好的，请提供需要改写的用户输入。
Baseline分析：长度比 1.14；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：好的，请提供需要改写的文本。
Optimized分析：长度比 1.0；问题：出现解释前缀/反问；语义重合过低(0.00)
结论：两者接近或需继续优化。

## 42
原始输入：别用语音播报开始说话，文字提示就够了
Baseline输出：任务：将用户输入改写为更清晰、可执行的文本。

强约束：
1. 仅重写、纠错或重排。禁止新增用户未提及的信息。
2. 禁止添加角色设定、身份描述、背景故事或解释性前后缀。
3. 禁止臆测功能、步骤、验收标准或风险项，除非原文已明确提及。
4. 保留原始意图、约束、数字和专有名词。
5. 修正常见错别字和不规范术语，不改变业务含义。
6. 仅输出最终改写结果，不输出任何说明。
7. 不使用星号、井号、反引号、方括号等特殊结构化符号。
8. 除非原文已包含，否则不得扩写为新的需求清单、功能拆解或实施方案。
9. 输出长度通常不超过原文的1.25倍。原文很短时，仅做必要纠错与术语规范。
10. 原文清晰时，优先最小改动，可直接返回轻微修正后的原句。

结构规则：
1. 原文为列表或分点时，保持该格式并优化语序。
2. 原文为自然语句时，输出为简洁段落，不强制套用模板。

场景补充：
1. 术语校正偏向Web Coding语境，例如 prompt、onboarding、frontend、backend、API。
2. 不新增未给出的技术方案。
3. 不自动补充角色设定、验收标准、风险评估、里程碑等模板化段落。请仅使用文字提示。
Baseline分析：长度比 28.72；问题：提示词泄露；长度偏长(28.72)；语义重合过低(0.00)
Optimized输出：请提供需要改写的文本。
Optimized分析：长度比 0.61；问题：出现解释前缀/反问；语义重合过低(0.00)
结论：优化版更好。

## 43
原始输入：输入中声音滴一下后仍显示输入中，这是状态机bug
Baseline输出：当输入时收到音频提示后，界面仍显示为输入中状态，这是一个状态机逻辑错误。
Baseline分析：长度比 1.5；问题：长度偏长(1.50)；语义重合过低(0.00)
Optimized输出：输入时发出提示音，但状态仍显示为输入中，这是状态机bug。
Optimized分析：长度比 1.21；问题：无
结论：优化版更好。

## 44
原始输入：修复崩溃：主线程按钮点击触发EXC_BAD_ACCESS
Baseline输出：修复主线程按钮点击时发生的EXC_BAD_ACCESS崩溃。
Baseline分析：长度比 1.07；问题：无
Optimized输出：修复主线程按钮点击触发 EXC_BAD_ACCESS 的崩溃。
Optimized分析：长度比 1.11；问题：无
结论：两者接近或需继续优化。

## 45
原始输入：把安装版签名固定，不要每次身份变导致权限失效
Baseline输出：固定安装版签名，避免因身份变更导致权限失效。
Baseline分析：长度比 1.0；问题：语义重合过低(0.00)
Optimized输出：固定安装版签名，防止身份变更导致权限失效。
Optimized分析：长度比 0.95；问题：语义重合过低(0.00)
结论：两者接近或需继续优化。

## 46
原始输入：Xcode里打开后先跑build再跑相关测试
Baseline输出：在Xcode中打开项目，先构建，再运行相关测试。
Baseline分析：长度比 1.09；问题：无
Optimized输出：在 Xcode 中打开后，先执行 build，再执行相关测试。
Optimized分析：长度比 1.41；问题：长度偏长(1.41)
结论：两者接近或需继续优化。

## 47
原始输入：这个需求保持原句，别扩写：支持修改快捷键
Baseline输出：支持修改快捷键
Baseline分析：长度比 0.35；问题：无
Optimized输出：Web Coding 术语规范：仅在不改变原意的前提下，纠正为 prompt、onboarding、frontend、backend、API。此需求保持原句，不作扩写：支持修改快捷键。
Optimized分析：长度比 4.6；问题：长度偏长(4.60)
结论：两者接近或需继续优化。

## 48
原始输入：把输入框位置放在屏幕中下，不要右上角
Baseline输出：将输入框放置在屏幕中下部，避免放置在右上角。
Baseline分析：长度比 1.22；问题：语义重合过低(0.00)
Optimized输出：输入框应置于屏幕中下部，而非右上角。
Optimized分析：长度比 1.0；问题：语义重合过低(0.00)
结论：两者接近或需继续优化。

## 49
原始输入：全屏场景下也要显示输入条，避免被窗口遮挡
Baseline输出：全屏场景下也要显示输入条，避免被窗口遮挡
Baseline分析：长度比 1.0；问题：最小改写/原样返回
Optimized输出：全屏场景下也要显示输入条，避免被窗口遮挡
Optimized分析：长度比 1.0；问题：最小改写/原样返回
结论：两者接近或需继续优化。

## 50
原始输入：如果用户取消输入，给轻提示后自动收起
Baseline输出：好的，请提供需要改写的用户输入。
Baseline分析：长度比 0.89；问题：出现解释前缀/反问；语义重合过低(0.00)
Optimized输出：(空)
Optimized分析：长度比 0.0；问题：无输出；语义重合过低(0.00)
结论：两者接近或需继续优化。
